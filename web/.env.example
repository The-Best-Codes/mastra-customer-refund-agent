# Base URL of the Mastra API server. Leave unset for local dev — Vite proxies
# `/support/*` requests to http://localhost:4111 (see vite.config.ts). Set this
# when the UI and the Mastra app are deployed separately.
VITE_API_BASE_URL=
